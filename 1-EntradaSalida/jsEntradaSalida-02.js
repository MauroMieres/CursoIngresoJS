/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	let nombre; //declaro una variable llamada "nombre"

	nombre = prompt("Ingrese su nombre"); //le pido al usuario que introduzca el nombre usando el cmd prompt

	alert(nombre);
}

